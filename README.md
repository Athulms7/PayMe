# PayTm
A simple Paytm-style money transfer app built with a React frontend, a Node.js + Express backend, and MongoDB as the database. The app allows users to check their balance and securely transfer money between accounts using MongoDB transactions to ensure atomicity and consistency.
